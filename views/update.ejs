<%- include('includes/head') %>
<%- include('includes/header') %>

<section class="section wb">
    <div class="container">
        <div class="row">

            <div class="col-lg-8 col-md-12 col-sm-12 col-xs-12">
                <div class="sidebar">
                    <div class="widget box-shadow--lightgrey">
                        <div class="alert alert-secondary text-center text-uppercase" role="alert">
                            Редакция на Рецепта
                        </div>

                        <!-- Display Reg. Errors -->
                        <% if (errors.length > 0) { %>

                            <% errors.forEach((error) => { %>
                                 <li class="alert alert-danger"><%= error %></li>
                             <% }) %>
 
                         <% } %>
                         <!-- END Display Reg. Errors -->

                        <hr class="invis">

                        <div class="recipe-image">
                            <img class="responsive-img" src="<%= recipe.image %>" allowfullscreen></img>
                        </div>

                        <hr class="invis">

                        <form action="/blog/recipe/<%= recipe._id %>/update" class="form-wrapper" method="POST" encType="multipart/form-data">
                            <input type="text" name="title" value="<%= recipe.title %>" class="form-control" placeholder="Заглавие:">
                            <input type="text" name="category" value="<%= recipe.category %>" class="form-control" placeholder="Категория:">
                            <input type="text" name="ingredients" value="<%= recipe.ingredients %>" class="form-control" placeholder="Продукти:">
                            <textarea name="content" class="form-control" placeholder="Начин на приготвяне:"><%= recipe.content %></textarea>
                            <input type="file" name="image" value="recipe.image" class="form-control-file">
                            <button type="submit" class="btn btn-primary mt-3">Запиши <i class="fa fa-envelope-open-o"></i></button>
                        </form>
                    </div>
                    <picture class="widget box-shadow--lightgrey">
                        <img src="/images/create-recipe.jpg" class="img-fluid img-thumbnail" alt="Create recipe">
                    </picture>
                </div>
            </div><!-- end col -->

            <!-- SIDE BAR -->
            <%- include('includes/sidebar') %>
        </div><!-- end row -->
    </div><!-- end container -->
</section>


<%- include('includes/footer') %>